const initialState = {
    users: [],
    user: {},
    loading: false
}

const userReducers = (state = initialState, action) => {
    switch (action.type) {
        // case value:
        default:
            return state;
    }
}

export default userReducers;